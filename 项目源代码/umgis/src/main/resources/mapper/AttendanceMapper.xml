<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unknown.hrms.dao.IAttendanceMapper">

    <select id="findByUserId" parameterType="Integer" resultType="com.unknown.hrms.entity.Attendance">
        select * from t_attendance where  user_id=#{user_id}
    </select>
</mapper>